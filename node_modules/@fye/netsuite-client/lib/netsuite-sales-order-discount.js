/* eslint-disable no-console */
// const debug = require('debug')('netsuite-sales-order-discount');
const NetSuiteSalesDiscount = require('./netsuite-sales-discount');

/**
 * NetSuiteSalesOrderDiscount class
 * @extends NetSuiteSalesDiscount
 */
class NetSuiteSalesOrderDiscount extends NetSuiteSalesDiscount {
  /**
   * Create an instance of the NetSuiteSalesOrderDiscount class
   * @param  {object} props
   * @param  {string} props.internalId           Internal Id
   * @param  {string} props.name                 Name
   * @param  {string} props.description          Description
   * @param  {number} props.amount               Amount
   * @param  {number} props.grossAmount          Gross amount
   * @param  {string} props.taxDetailsReference  Tax details reference
   */
  constructor({ internalId, name, description, amount, grossAmount, taxDetailsReference }) {
    super({
      internalId,
      name,
      description,
      amount,
      grossAmount,
      taxDetailsReference,
    });
  }

  /**
   * Compare against another NetSuiteSalesOrderDiscount object to see if they're equal
   * @param {NetSuiteSalesOrderDiscount} netsuiteSalesOrderDiscount  The object to compare against
   * @return {boolean} true if they're equal, else returns false
   */
  equals(netsuiteSalesOrderDiscount) {
    if (super.equals(netsuiteSalesOrderDiscount)) {
      return true;
    }
    return false;
  }

  /**
   * Output the properties to the console
   */
  dump() {
    super.dump();
  }
}

module.exports = NetSuiteSalesOrderDiscount;
